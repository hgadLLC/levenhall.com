---
---

<div class="bio-modal-overlay" id="adamBioModal" onclick="closeAdamBio(event)">
    <div class="bio-modal" onclick="event.stopPropagation()">
        <button class="bio-modal-close" onclick="closeAdamBio()">&times;</button>

        <div class="bio-modal-header">
            <img src="/images/team/adam-leslie.jpg" alt="Adam Leslie" class="bio-modal-photo">
            <div class="bio-modal-header-text">
                <h2>Adam J.P. Leslie</h2>
                <div class="bio-modal-role">Chairman & CEO</div>
                <div class="bio-modal-tagline">Strategic Intelligence Advisor &middot; National Security &middot; Government Architecture</div>
            </div>
        </div>

        <div class="bio-modal-body">
            <div class="bio-section-label">Background</div>
            <p class="bio-text">
                Adam Leslie's career spans sovereign intelligence service, diplomatic engagement, military aviation, executive nonprofit leadership, and U.S. government market architecture. He operates within the upper tier of independent national security strategists in Washington, DC &mdash; retained for judgment, structural insight, and disciplined execution in high-consequence environments.
            </p>

            <div class="bio-section-label">Advisory Domains</div>
            <div class="bio-domains-grid">
                <div class="bio-domain-item"><span>National security &amp; geopolitical strategy</span></div>
                <div class="bio-domain-item"><span>Defense sector positioning &amp; sovereign risk assessment</span></div>
                <div class="bio-domain-item"><span>U.S. government market entry &amp; contract architecture</span></div>
                <div class="bio-domain-item"><span>CSO, OTA &amp; rapid procurement pathways</span></div>
                <div class="bio-domain-item"><span>Executive &amp; board-level strategic counsel</span></div>
                <div class="bio-domain-item"><span>Sensitive opportunity structuring &amp; cross-border strategy</span></div>
            </div>
        </div>

        <div class="bio-contact-bar">
            <div class="bio-contact-links">
                <a href="mailto:adam@levenhall.com" class="bio-contact-link">
                    <svg viewBox="0 0 24 24"><path d="M20 4H4c-1.1 0-2 .9-2 2v12c0 1.1.9 2 2 2h16c1.1 0 2-.9 2-2V6c0-1.1-.9-2-2-2zm0 4l-8 5-8-5V6l8 5 8-5v2z"/></svg>
                    adam@levenhall.com
                </a>
                <a href="tel:+12025682101" class="bio-contact-link">
                    <svg viewBox="0 0 24 24"><path d="M6.62 10.79c1.44 2.83 3.76 5.14 6.59 6.59l2.2-2.2c.27-.27.67-.36 1.02-.24 1.12.37 2.33.57 3.57.57.55 0 1 .45 1 1V20c0 .55-.45 1-1 1-9.39 0-17-7.61-17-17 0-.55.45-1 1-1h3.5c.55 0 1 .45 1 1 0 1.25.2 2.45.57 3.57.11.35.03.74-.25 1.02l-2.2 2.2z"/></svg>
                    +1 (202) 568-2101
                </a>
                <a href="https://www.linkedin.com/in/adamjpleslie/" target="_blank" rel="noopener noreferrer" class="bio-contact-link">
                    <svg viewBox="0 0 24 24"><path d="M19 3a2 2 0 012 2v14a2 2 0 01-2 2H5a2 2 0 01-2-2V5a2 2 0 012-2h14m-.5 15.5v-5.3a3.26 3.26 0 00-3.26-3.26c-.85 0-1.84.52-2.32 1.3v-1.11h-2.79v8.37h2.79v-4.93c0-.77.62-1.4 1.39-1.4a1.4 1.4 0 011.4 1.4v4.93h2.79M6.88 8.56a1.68 1.68 0 001.68-1.68c0-.93-.75-1.69-1.68-1.69a1.69 1.69 0 00-1.69 1.69c0 .93.76 1.68 1.69 1.68m1.39 9.94v-8.37H5.5v8.37h2.77z"/></svg>
                    LinkedIn
                </a>
            </div>
            <a href="mailto:adam@levenhall.com?subject=Advisory%20Engagement%20Inquiry" class="btn btn-gold">Contact Adam</a>
        </div>

        <div class="bio-selective-note">
            Engagements are selective and subject to conflict and compliance review.
        </div>
    </div>
</div>

<script is:inline>
    function openAdamBio() {
        document.getElementById('adamBioModal').classList.add('active');
        document.body.style.overflow = 'hidden';
    }

    function closeAdamBio(e) {
        if (e && e.target !== e.currentTarget) return;
        document.getElementById('adamBioModal').classList.remove('active');
        document.body.style.overflow = '';
    }

    document.addEventListener('keydown', function(e) {
        if (e.key === 'Escape') closeAdamBio();
    });
</script>

<style is:global>
    .bio-modal-overlay {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: rgba(15, 31, 58, 0.85);
        z-index: 10000;
        display: flex;
        align-items: center;
        justify-content: center;
        opacity: 0;
        visibility: hidden;
        transition: opacity 0.35s ease, visibility 0.35s ease;
        backdrop-filter: blur(6px);
        -webkit-backdrop-filter: blur(6px);
    }

    .bio-modal-overlay.active {
        opacity: 1;
        visibility: visible;
    }

    .bio-modal {
        background: #fff;
        width: 94%;
        max-width: 720px;
        max-height: 88vh;
        overflow-y: auto;
        position: relative;
        transform: translateY(30px);
        transition: transform 0.4s ease;
    }

    .bio-modal-overlay.active .bio-modal {
        transform: translateY(0);
    }

    .bio-modal-close {
        position: absolute;
        top: 1.2rem;
        right: 1.4rem;
        width: 36px;
        height: 36px;
        background: none;
        border: 1px solid rgba(255,255,255,0.2);
        cursor: pointer;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.3rem;
        color: #fff;
        transition: all 0.25s ease;
        z-index: 10;
    }

    .bio-modal-close:hover {
        background: var(--burgundy);
        border-color: var(--burgundy);
    }

    .bio-modal-header {
        background: var(--deep-navy);
        padding: 3rem 3rem 2.4rem;
        display: flex;
        align-items: center;
        gap: 2.2rem;
    }

    .bio-modal-photo {
        width: 120px;
        height: 120px;
        border-radius: 50%;
        object-fit: cover;
        border: 3px solid var(--gold);
        flex-shrink: 0;
    }

    .bio-modal-header-text h2 {
        font-family: 'Crimson Text', serif;
        font-size: 1.8rem;
        color: #fff;
        font-weight: 700;
        margin-bottom: 0.3rem;
    }

    .bio-modal-role {
        font-size: 0.82rem;
        color: var(--gold);
        font-weight: 600;
        letter-spacing: 1.5px;
        text-transform: uppercase;
        margin-bottom: 0.6rem;
    }

    .bio-modal-tagline {
        font-size: 0.85rem;
        color: var(--silver);
        font-weight: 300;
        line-height: 1.5;
    }

    .bio-modal-body {
        padding: 2.4rem 3rem 2.8rem;
    }

    .bio-section-label {
        font-family: 'Source Sans Pro', sans-serif;
        font-weight: 600;
        font-size: 0.68rem;
        letter-spacing: 3px;
        text-transform: uppercase;
        color: var(--burgundy);
        margin-bottom: 1rem;
        padding-bottom: 0.5rem;
        border-bottom: 1px solid rgba(30, 58, 95, 0.1);
    }

    .bio-text {
        font-size: 0.95rem;
        color: #444;
        line-height: 1.75;
        margin-bottom: 2.2rem;
    }

    .bio-domains-grid {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 0.8rem;
    }

    .bio-domain-item {
        display: flex;
        align-items: flex-start;
        gap: 0.6rem;
        padding: 0.7rem 0.9rem;
        background: rgba(15, 31, 58, 0.03);
        border-left: 2px solid var(--gold);
    }

    .bio-domain-item span {
        font-size: 0.88rem;
        color: var(--navy);
        line-height: 1.45;
    }

    .bio-contact-bar {
        background: var(--deep-navy);
        padding: 1.6rem 3rem;
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 1rem;
        flex-wrap: wrap;
    }

    .bio-contact-links {
        display: flex;
        align-items: center;
        gap: 1.8rem;
    }

    .bio-contact-link {
        font-size: 0.82rem;
        color: rgba(255, 255, 255, 0.8);
        text-decoration: none;
        display: flex;
        align-items: center;
        gap: 0.4rem;
        transition: color 0.25s ease;
    }

    .bio-contact-link:hover { color: var(--gold); }

    .bio-contact-link svg {
        width: 14px;
        height: 14px;
        fill: currentColor;
        flex-shrink: 0;
    }

    .bio-selective-note {
        font-size: 0.78rem;
        color: #999;
        font-style: italic;
        text-align: center;
        padding: 1rem 3rem 1.6rem;
        border-top: 1px solid rgba(30, 58, 95, 0.06);
    }

    @media (max-width: 600px) {
        .bio-modal-header {
            flex-direction: column;
            text-align: center;
            padding: 2.2rem 1.8rem 1.8rem;
            gap: 1.2rem;
        }
        .bio-modal-body { padding: 1.8rem 1.8rem 2rem; }
        .bio-domains-grid { grid-template-columns: 1fr; }
        .bio-contact-bar {
            flex-direction: column;
            text-align: center;
            padding: 1.4rem 1.8rem;
        }
        .bio-contact-links { flex-direction: column; gap: 0.8rem; }
    }
</style>
